# vms-ui (frontend)

This repo is part of the `D:\VMS-BE-FE` workspace. The AI working agreements
(what to do before starting a task, unit testing workflow, architecture conventions)
live two levels up in `../../ai-context` — start with `../../ai-context/AI-WORKFLOW.md`
and `../../ai-context/ARCHITECTURE-FRONTEND.md`.

The `/vms-unittesting` skill is defined at the workspace root
(`D:\VMS-BE-FE\.claude\skills`). If it isn't available from inside this directory,
run Claude Code from `D:\VMS-BE-FE` instead so both `VMS` and `VMS-UI` are in scope.
